version: "3.7"
services:
  yaco_fashion:
    container_name: yaco_fashion
    build:
      context: .
    env_file:
      - .env
    expose:
      - 8080
    networks:
      - yaco_net

  nginx:
    container_name: nginx
    build:
      context: ./nginx
    ports:
      - "80:80"
      - "8080:8080"
    depends_on:
      - yaco_fashion
    restart: on-failure
    networks:
      - yaco_net

networks:
  yaco_net:
    driver: bridge
    name: yaco_net